# Surfaces 1-16 Closure (2026-05-12)

Scope: close upstream queue tranche 1-16 without duplicate implementation and keep Rust-first parity policy intact.

## Source Policy Applied

For external research/sourcing workflows, excluded by policy:
- GitHub Copilot as a primary feature-source authority
- Anthropic docs/blog as a primary feature-source authority
- modelcontextprotocol.io as a primary feature-source authority

This tranche was executed from upstream queue evidence + local Rust code verification only.

## Surgical Plan

1. Generate current actionable queue from upstream and freeze tranche 1-16.
2. For each item, verify one of:
   - already mirrored in local file surfaces,
   - already implemented in Rust-equivalent behavior,
   - metadata/no-op relative to Rust fork architecture.
3. Codify disposition in machine-readable queue policy so future runs do not re-open the same work.
4. Rebuild queue report and confirm tranche 1-16 has no `pending` duplicates.

## Implemented Changes

- Hardened queue generator (`scripts/generate-upstream-patch-queue.py`):
  - Added override file support (`--overrides`, default `docs/parity/queue-overrides.json`).
  - Added prefix-aware SHA override resolution.
  - Added mirrored-file-state classification (`mirrored`) when local and upstream file blobs are already identical.
  - Added subject-pattern supersede defaults for release metadata/review-only noise.
  - Added explicit `classification_rule` output for auditability.
- Added `docs/parity/queue-overrides.json` with Rust-equivalence mappings for tranche 1-16 commits that touched Python-only upstream surfaces.

## 1-16 Disposition Result

After regeneration, tranche 1-16 is fully closed (no pending duplicates):

1. `d8cc85dcdccf` superseded (review-only)
2. `3d90292eda55` superseded (Rust models.dev alias handling already covered)
3. `77f99c4ff445` superseded (AUTHOR_MAP release metadata)
4. `8b1ff55f5382` superseded (Rust WeCom mention-strip behavior already covered)
5. `85cc12e2bd55` superseded (AUTHOR_MAP release metadata)
6. `92e4bbc201e6` mirrored (docker docs already synced)
7. `fa47cbd45671` superseded (AUTHOR_MAP release metadata)
8. `156b3583206d` mirrored (cron docs already synced)
9. `48dc8ef1d158` mirrored (cron docs already synced)
10. `e8cba18f77c2` superseded (AUTHOR_MAP release metadata)
11. `15efb410d035` superseded (Nix module path not part of this fork runtime surface)
12. `5e76c650bbae` superseded (AUTHOR_MAP release metadata)
13. `22afa066f838` superseded (Rust cron runner already guards object-only control payload)
14. `1c532278ae70` superseded (AUTHOR_MAP release metadata)
15. `738d0900fddd` superseded (Rust auxiliary transport path already typed)
16. `f4612785a485` superseded (Rust Anthropics normalization path already typed/tested)

## Verification

- Re-ran generator with current overrides:
  - `python3 scripts/generate-upstream-patch-queue.py --max-commits 300 --out-json docs/parity/latest-upstream-patch-queue.json --out-md docs/parity/latest-upstream-patch-queue.md`
- Confirmed tranche 1-16 has no `pending` entries.

