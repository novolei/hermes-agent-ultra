// Content stub for uclaw components/home-office/HomeOfficeView — deferred to a later plan (Plan PV.d).
import * as React from 'react'

export function HomeOfficeView(): React.ReactElement {
  return <span data-deferred-stub="home-office-view" hidden />
}
