// Content stub for uclaw's views/Workspace/WorkspaceShell (~1.2 KLOC cluster, not yet ported).
// MainArea renders this until the real WorkspaceShell cluster lands.
import * as React from 'react'
export function WorkspaceShell(_props: { [key: string]: unknown }): React.ReactElement {
  return (
    <div data-deferred-stub="WorkspaceShell" className="flex h-full items-center justify-center p-8 text-sm text-muted-foreground">
      工作区外壳将在后续 PR 中接入。
    </div>
  )
}
