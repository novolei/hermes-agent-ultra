// Content-stub for uclaw's KaleidoscopeShell (config-flow surface: personas /
// apps / skills / integrations / memory / output). Deferred to a future port;
// AppShell mounts this placeholder when topLevelView === 'kaleidoscope'. Marked
// data-deferred-stub (NOT data-stub) so AgentView stub-completion tests' [data-stub]
// count stays 0.
import * as React from 'react'

export function KaleidoscopeShell(): React.ReactElement {
  return <div data-deferred-stub="kaleidoscope-shell" hidden />
}
