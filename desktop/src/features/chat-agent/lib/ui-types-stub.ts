/**
 * Stub for uclaw's `@/components/app-shell/RightSidePanel` type-only import
 * inside `agent-atoms.ts`. Plan 3 (App Shell) will provide the real
 * component AND this type; until then, this stub exists so the
 * state-machine port type-checks.
 */
export type ActiveTab = 'files' | 'teams' | 'plan' | 'trajectory' | 'browser'
