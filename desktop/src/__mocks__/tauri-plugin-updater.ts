/**
 * Stub for @tauri-apps/plugin-updater — used in test environments where
 * the Tauri runtime is not available. Provides the minimal surface that
 * atoms/updater.ts imports so Vite's import-analysis pass can resolve it.
 */
export async function check(): Promise<null> {
  return null
}
