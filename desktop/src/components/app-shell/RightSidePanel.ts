/**
 * RightSidePanel — Desktop stub
 *
 * Minimal type stub for ActiveTab, co-located with the per-workspace
 * right-panel tab memory atom in agent-atoms.ts. The real component
 * will be ported in Plan 2b.2.b/c.
 */

/** Available tabs in the right-side panel. */
export type ActiveTab = 'files' | 'teams' | 'plan' | 'trajectory' | 'browser'
